%h1= t("title.game_index")
%ul.games
  - @games.each do |game|
    %li.game-title= link_to game.title, game_path(game)
    = image_tag(game.picture_url, size: "200x200") if game.picture.present?
    %hr/
= paginate @games
