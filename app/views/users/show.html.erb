<h1>ようこそGame Shareへ！</h1>
<%= @user.name %>
<% if @user.games.any? %>
  <h3>タイトル一覧</h3>
  <% @games.each do |game| %>
    <p><%=link_to game.title, game_path(game) %></p>
    <hr>
  <% end %>
<% end %>
<%= link_to "トップへ", root_path %>
<%= link_to "ゲームを登録する", new_game_path %>


